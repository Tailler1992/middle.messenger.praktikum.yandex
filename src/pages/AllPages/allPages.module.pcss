.nav {
  position: fixed;
  top: 0;
  left: 20%;
  display: flex;
  justify-content: center;
}

.list {
  padding: 10px;
  display: flex;
  gap: 10px;

  li {
    padding: 5px;
    background: var(--button-primary-color-bg);
    border-radius: 5px;
  }

  a {
    color: #ffffff;
    font-weight: 400;
  }
}
